# 2026-07-30 工作日志

## 飞行棋棋盘调整：2x2 基地 + 放大格子

按用户反馈"飞机场太大了，只要2x2，4个格子就好，地图放大太小了"，修改 AeroChessGame.java：

- 网格从 25×25 缩减到 20×20（基地变小后整体更紧凑）
- CELL 从 26px 放大到 38px（格子更大更清晰）
- PAD 从 18px 放大到 20px
- 四角基地从 5×5 改为 2×2（仅 4 格放 4 个停机位）
  - 绿(0,0)-(1,1), 红(0,18)-(1,19), 橙(18,0)-(19,1), 蓝(18,18)-(19,19)
- drawBaseHangars 重写：2×2 基地画一个圆角矩形背景框 + 4 个停机圆圈 + 棋子
- 外圈赛道 ring 在 row/col 4 和 16（与基地间隔 2 行/列空白）
- 航道坐标调整：绿 col10 rows5-9, 红 row10 cols11-15, 蓝 col10 rows11-15, 橙 row10 cols5-9
- 中心终点 (10,10)
- 窗口大小从 1000×800 放大到 1050×870
- HANGAR_D 从 22 放大到 28
- drawCenterCell 文字和圆圈尺寸适配 CELL=38
- 赛道颜色交替：每格一色，循环 红→橙→蓝→绿 (i % 4)

重新编译打包，替换 fishgrab.jar，备份 bak18。
更新 飞行棋格子坐标.txt。

## 飞行棋棋盘简化：去掉中间四格正方形和加油站虚线

按用户要求"去掉中间那几个四格正方形，去掉加油站这几个字和那一条虚线"：
- 删除 T_XARM 类型常量和 initCellMap 中 4 个 2×2 XARM 色块定义
- 删除 drawXArmCell 方法
- 删除 paintComponent 中 T_XARM 的 switch 分支
- 删除整个 drawFlyLines 方法（虚线 + "加油站"文字）
- 删除 paintComponent 中 drawFlyLines 调用
重新编译打包，替换 fishgrab.jar，备份 bak19。

## 飞行棋棋盘放大

按用户要求"地图可以再大一些"：
- CELL 从 38px 放大到 46px
- 窗口大小从 1050×870 放大到 1200×1000
重新编译打包，替换 fishgrab.jar，备份 bak20。

## 飞行棋基地固定小尺寸

按用户要求"周围四个角的机场不用放大"：
- 新增 BASE_CELL=34 常量，基地使用固定尺寸，居中在 2x2 CELL 区域内
- HANGAR_D 从 28 改回 24，停机圆圈也缩小
- 停机棋子尺寸 HANGAR_D+6（原 HANGAR_D+8）
- 赛道 CELL=46 不变，只有基地保持小巧
重新编译打包，替换 fishgrab.jar，备份 bak21。

## 飞行棋棋盘按参考图调整颜色与中心

按用户要求"尽可能把地图做的跟参考图类似，颜色采用红黄蓝绿"：
- 将橙色改为黄色（PLAYER_COLORS[1] = YELLOW），颜色顺序：红/黄/蓝/绿
- 航道改为白底 + 远端彩色端点（更接近参考图）
- 中心终点从红色"终"字圆圈改为四色扇形（左上绿、右上红、右下蓝、左下黄）
重新编译打包，替换 fishgrab.jar，备份 bak22。
关于"12边形"待用户进一步确认后再改。

## 飞行棋棋盘改十字形（21×21）

按用户要求"棋盘路径形状是十字/十字形，12边形不是正方形，21×21大小"：
- 从20×20方形棋盘完全重写为21×21十字形棋盘
- N=21, CELL=40, PAD=20, ARM_W=5, 中心5×5(rows8-12 cols8-12)
- `inCross(r,c)` 判断十字形区域：(r在8-12) || (c在8-12)
- 外圈80步赛道沿十字形轮廓顺时针，4个桥接格(8,12)/(12,12)/(12,8)/(8,8)
- 每色等距20步：绿起点(0,10)→红(10,20)→蓝(20,10)→黄(10,0)
- 颜色交替：(i/5)%4，起点格覆盖为对应颜色
- 四色航道各9格：绿col10 rows1-9, 红row10 cols11-19, 蓝col10 rows11-19, 黄row10 cols1-9
- 中心(10,10)四色扇形
- 基地BASE_SIZE=60px固定，放在臂端
- 十字形背景：竖向fillRoundRect + 横向fillRoundRect
- 修复for循环缺少大括号的编译错误（4处CELL_COLOR赋值在循环外）
- 重新编译打包，替换fishgrab.jar，备份bak23

## 飞行棋基地外移，不重叠地图

按用户反馈"四格准备点不应该重叠在地图里"：
- 基地从臂端内部移到十字形地图外侧
- BASE_SIZE 改为 2×CELL = 80px（真正的2×2格基地）
- 新增 BASE_GAP=14, OUTER_PAD=BASE_SIZE+BASE_GAP+PAD
- BOARD_PX = N*CELL + OUTER_PAD*2，棋盘四周留出基地空间
- 绿基地：上侧 (ox+9*CELL, oy-BASE_SIZE-BASE_GAP)
- 红基地：右侧 (ox+N*CELL+BASE_GAP, oy+9*CELL)
- 蓝基地：下侧 (ox+9*CELL, oy+N*CELL+BASE_GAP)
- 黄基地：左侧 (ox-BASE_SIZE-BASE_GAP, oy+9*CELL)
- drawBaseHangars 改为直接接收左上角坐标，不再做居中计算
- HANGAR_D 从22放大到26
- 窗口从1100×950放大到1250×1080
- 重新编译打包，替换fishgrab.jar，备份bak24

## 飞行棋棋盘改为标准正方形外圈赛道

按用户参考图"按我这张图来改地图，四个白色的格子为四种颜色棋子的起点"：
- 外圈赛道从十字形改为正方形外圈（沿21×21网格四边）
- 赛道80格顺序：(0,10)→右上顶边→右侧边→(10,20)→...→底边→(20,10)→...→左侧边→(10,0)→...→回到(0,10)
- 起点：四边中点白色格 (0,10)/(10,20)/(20,10)/(10,0)
- 新增 T_START 类型，drawStartCell 画白底+彩色圆点+彩色边框
- 四个基地移到四角（与参考图一致）：左上黄、右上绿、左下蓝、右下红
- 基地覆盖 2×2 格角落区域
- 棋盘背景改为完整21×21正方形
- 删除十字形背景绘制和 inCross 过滤（改为遍历全部格子）
- 赛道颜色改为每2格一循环：(i/2)%4
- 窗口从1250×1080调整到1100×980
- 重新编译打包，替换fishgrab.jar，备份bak25

## 飞行棋棋盘改回十字形轮廓 + 四角基地分离

按用户要求"严格按照参考图形状，该分开就分开，格子颜色要正确"：
- 外圈赛道从正方形外圈改回十字形轮廓（12边形），80格沿十字周界顺时针
- 赛道坐标恢复原始设计：绿→红→蓝→黄各20步等距
- 颜色改为顺时针序CW_ORDER={3,0,2,1}(绿红蓝黄)，每5格一色段
- CELL_COLOR公式: CW[(section + stretch) % 4]，section=i/20, stretch=(i%20)/5
- 四角基地完全与赛道分离：放在十字形外侧的4个8×8角区域
  - 左上角(0-7行,0-7列)=黄色基地
  - 右上角(0-7行,13-20列)=绿色基地
  - 左下角(13-20行,0-7列)=蓝色基地
  - 右下角(13-20行,13-20列)=红色基地
- drawBaseHangars改为drawCornerBase：传入角起始行列，画7×7居中彩色矩形+4停机位
- drawPathCell简化：移除内圆，只画纯色格子+白边框
- 棋盘背景改为：先画整体浅色方形→四角彩色基地→十字浅色覆盖→格子绘制
- HANGAR_D放大到30，BASE_PAD=CELL/2
## 飞行棋外圈颜色改为单格循环

按用户反馈"格子和我图不一样，不要自作聪明"：
- 外圈赛道颜色从"每5格一段纯色"改为"一格一循环"
- 颜色序列改为 红(0)→蓝(2)→黄(1)→绿(3)
- CELL_COLOR公式: CYCLE[(i + 3) % 4]，CYCLE={0,2,1,3}
  - i=0（绿起点）仍由T_START覆盖为白色
  - i=1（绿起点右侧第一格）= 红色
- drawPathCell增加白色中心圆点，与参考图彩色格带白圆一致
- 重新编译打包，替换fishgrab.jar，备份bak27
- 注明：如仍有单格颜色不符，需用户指出具体格子再精确调整

## 飞行棋匹配改为1秒后直接机器人（调试模式）

按用户要求"匹配暂时先改成1s匹不到人直接机器人方便调试"：
- startMatchTimer()：matchCountdown 从60改为1
- 倒计时到0后：自动用机器人填满空位(fillWithBots)，然后直接调用startGame()启动游戏
- 不再等待服务器返回STARTED状态，用本地System.currentTimeMillis()作为seed
- fillWithBots()：遍历maxPlayers，空位设为"机器人1/2/3"
- 重新编译打包，替换fishgrab.jar，备份bak28

## 飞行棋棋盘尺寸重新设计（按用户要求）

按用户要求"顶端7格、短边5格、航道6格"重新设计：
- N从21改为19，中间在(9,9)
- ARM_W从5改为7（顶端平面7格宽，中心7×7）
- C0从8改为6，C1保持12（中心rows/cols 6-12）
- 外圈从80改为72格（4×18步等距）
- HOME_RUNWAY_CELLS从9改为6
- TOTAL_PROGRESS从89改为78(72+6)
- 起点位置：绿(0,9), 红(9,18), 蓝(18,9), 黄(9,0)
- START_INDEX={18,54,36,0}（每段18步）
- 航道6格：绿col9 rows1-6, 红row9 cols12-17, 蓝col9 rows12-17, 黄row9 cols1-6
- 中心终点(9,9)
- 四角基地调整为6×6角区域（5×5基地，BASE大小从7*CELL改为5*CELL）
- drawAllPieces中心坐标从(10,10)改为(9,9)
- 飞行线参数调整：FLY_FROM_STEP=4, FLY_TO_STEP=13
- 窗口大小从1100×980改为1050×940
- 重新编译打包，替换fishgrab.jar，备份bak29

## 飞行棋短边从5格改为3格（棋盘整体重新设计）

按用户要求"顶面旁两条小边应该只有3格的长度"：
- N从19改为15（3格短边+7×7中心+3格短边+端面=15）
- C0从6改为4，C1从12改为10（中心rows/cols 4-10，7×7）
- CELL从40改为55（放大格子保持棋盘视觉大小）
- 外圈从72改为56格（每色14步等距）
- TOTAL_PROGRESS从78改为62(56+6)
- 起点：绿(0,7), 红(7,14), 蓝(14,7), 黄(7,0)
- START_INDEX={14,42,28,0}
- 端面7格，短边3格，航道6格
- 四角基地改为4×4角区域（3×3基地）
- 中心从(9,9)改为(7,7)
- 窗口从1050×940改为1050×900
- 重新编译打包，替换fishgrab.jar，备份bak30

## 飞行棋起点和颜色重新设定（用户精确指定）

按用户要求"绿起点(0,10)红起点(10,14)蓝起点(14,4)黄起点(4,0)，(4,1)蓝色，顺时针蓝黄绿红铺色"：
- 四起点在56格路径中的索引：绿idx3, 红idx17, 蓝idx31, 黄idx45
- START_KEY: 红10*15+14=164, 黄4*15+0=60, 蓝14*15+4=214, 绿0*15+10=10
- START_INDEX: {17, 45, 31, 3}（绝对路径索引）
- 颜色循环：CYCLE={2,1,3,0}(蓝黄绿红)，无视起点白色格
- 非起点格计数+偏移2：CELL_COLOR[r][c] = CYCLE[(nonStartPos+2)%4]
  - 验证：(4,1)在路径idx46, nonStartPos=42 → CYCLE[(42+2)%4]=CYCLE[0]=蓝 ✓
- 进度系统改为**相对进度**（每色从0开始，不再用绝对路径索引）
  - takeOff: pieceProgress=0（不再设START_INDEX）
  - positionToCellKey: absIndex=(START_INDEX[color]+progress)%OUTER_CELLS
  - colorStartProgress: 返回0（不再返回START_INDEX）
  - movePiece: 超过终点回退逻辑移到if外（修复跑道overshoot bug）
  - checkFlyLine: 用相对进度FLY_FROM_STEP=4, FLY_TO_STEP=32(半圈28+4)
- 航道仍沿中间线(col7/row7)，各6格
- 重新编译打包，替换fishgrab.jar，备份bak31

