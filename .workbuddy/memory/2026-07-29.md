# 2026-07-29 工作日志

## 飞行棋模式开发

为摸鱼中心新增飞行棋游戏模式入口，包含主页和匹配房间。

### 新增文件
- `src/AeroChessHome.java` — 飞行棋主页，参考 TetrisHome 布局，仅有一个"开始游戏 (四人)"按钮，点击后直接以四人模式调用 `ServerClient.duelCreate(currentUser, "经典", 4, "飞行棋")` 创建房间并打开匹配房间。无人数选择对话框。
- `src/AeroChessMatchRoom.java` — 飞行棋匹配房间，完整复用 TetrisMatchRoom/MineMatchRoom 的对决匹配架构：玩家槽位、匹配/准备按钮、好友邀请、轮询、服务器推送接收、房间状态同步。gameType 为 "飞行棋"。游戏启动时暂显示"即将上线"提示。

### 修改文件
- `src/FishGrabbingHome.java` — 在俄罗斯方块按钮下方新增"飞行棋"按钮，点击跳转 AeroChessHome。
- `src/MessageCenter.java` — 在所有对决推送路由点（DUEL_STATE、DUEL_MATCH_MOVE、DUEL_BOTS_JOINED、DUEL_GAME_START、DUEL_READY_CHANGE）添加 AeroChessMatchRoom 的接收调用；在邀请接受逻辑中添加 "飞行棋" gameType 路由。
- `src/AeroChessMatchRoom.java` — 匹配完成后跳转到 `AeroChessGame` 游戏页面。

### 编译验证
- `javac -encoding UTF-8 -d out` 全部源文件编译通过，无错误。

## 后台进程残留问题修复

修复程序窗口关闭后后台进程仍运行的问题：
- `FishGrabbingHome.java`：关闭时调用 `MessageCenter.stopGlobalPush()` + `ServerClient.shutdown()` + `dispose()` + `System.exit(0)`；隐藏时 `hideActiveInstance()` 先停止 `redrawTimer`；显示时 `showActiveInstance()` 恢复 `redrawTimer`；构造函数中注册 `ServerClient.registerShutdownHook()`
- `ServerClient.java`：新增 `shutdown()` 方法（关闭Socket连接）、`registerShutdownHook()` 方法（注册JVM退出时自动关闭连接）、`shuttingDown` 标志位
- `AeroChessHome.java`：返回主页按钮改为 `dispose()` + `FishGrabbingHome.showActiveInstance()`；windowClosing 也调用 `FishGrabbingHome.showActiveInstance()`
- `AeroChessMatchRoom.java`：关闭和退出按钮统一改为 `FishGrabbingHome.showActiveInstance()`；确保 `stopMatchTimer()` 在关闭时被调用
- `AeroChessGame.java`：关闭时如果匹配房间已不存在，fallback 到 `FishGrabbingHome.showActiveInstance()`
- `MessageCenter.java`：隐藏摸鱼中心统一调用 `FishGrabbingHome.hideActiveInstance()`（会停止Timer）

## 打包替换
重新打包 fishgrab.jar 并替换到 FishGrabbingAssistant/app，备份 bak12。

## 飞行棋棋盘按参考图精确还原

完全重写 `src/AeroChessGame.java` 的棋盘绘制部分，按用户提供的参考图精确还原：
- 15×15 标准飞行棋网格，米色棋盘底
- 四角基地：左上绿、右上红、左下橙、右下蓝，各含 4 个带飞机图标的圆形机位
- 每个基地标注 "ready" 文字，红基地额外标注 "请投骰"
- 外圈 52 格路径：每格颜色和字母按参考图精确配置（A-S、W 共 20 个字母标记）
- 四色各 6 格专属航道通向中心
- 中心终点：四色星芒 + 红色圆心 + 白色 "终" 字
- 飞行线用虚线绘制，并标注 "加油站" 文字
- 保留原有飞行棋游戏逻辑（摇 6 起飞、移动、飞行线跳跃、撞击回准备区、胜负判定）

重新编译打包，替换 `FishGrabbingAssistant/app/fishgrab.jar`，备份 bak13。

## 飞行棋棋盘二次重设（按新参考图 13x13 + X 中心）

完全重写 `src/AeroChessGame.java` 的棋盘绘制，按用户提供的新参考图设计：

**新棋盘结构（13x13 网格）**:
- 4 个 3x3 角基地：左上绿、右上红、左下橙、右下蓝，每基地含 2x2 hangar 圆圈+准备区棋子
- 外圈 4 条边，每条边 3 直格 + 1 三角格 + 3 直格 = 7 个外圈格，加翼格
- 三角格（左下/右上对角分割）按红+绿、橙+蓝等组合体现相邻色融合
- 4 条专属航道：绿（横 row 8 cols 1-5）、蓝（横 row 8 cols 7-11）、红（竖 col 5 rows 1-5）、橙（竖 col 7 rows 7-11），每色 5 格
- 中心 3x3 X 形：4 色臂（红上、橙下、绿左、蓝右）+ 4 角 + 中心红圆
- 4 条飞行线虚线 + "加油站" 文字标注

**外圈路径 52 步**：
- `OUTER_PATH_CELLKEY[]` 数组构建顺时针 52 个 cellKey（顶边→右臂→底边→左臂）
- `START_KEY[color]` 索引：红 (0,7)、橙 (12,4)、蓝 (5,12)、绿 (0,4)
- `FLY_FROM = 4`、`FLY_TO = 18` 飞行线触发

**页面尺寸**：900x730，棋盘用 13x46=598 px + 28 px padding ≈ 626 px

游戏规则保持：摇 6 起飞、可叠放同色、撞回异色、终点回退、四子到终点获胜。备 bak14。

## 飞行棋棋盘三次重设（25x25 独立基地 + 交替色赛道）

按用户反馈“基地与赛道没有连在一起、赛道颜色应交替出现”，再次重写 `src/AeroChessGame.java`：

- 棋盘扩大到 25x25 网格，每格 26 像素，窗口 1000x800
- 四角 5x5 基地：左上绿、右上红、左下橙、右下蓝；基地与赛道之间隔一整行/列空白，完全独立
- 外圈 48 步赛道位于 row=6 / row=18 / col=6 / col=18，每格颜色按 [绿,红,橙,蓝] 循环交替
- 四个角点分别为对应色起飞三角：绿(6,6)、红(6,18)、蓝(18,18)、橙(18,6)
- 四色专属航道各 5 格，从外圈指向中心
- 中心终点 (12,12)：红色圆心 + “终” 字
- 保留摇 6 起飞、飞行线、撞击、叠放、胜负判定等逻辑
- 修正 `checkWin` 中变量名 `c` → `color` 的编译错误
- 生成 `飞行棋格子坐标.txt` 列出所有关键坐标与颜色

重新编译全部源文件，打包 `fishgrab.jar`，替换 `FishGrabbingAssistant/app/fishgrab.jar`，备份 bak17。

